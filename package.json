{
  "name": "web-ext-manifest-gen",
  "version": "1.4.1",
  "@pika/pack": {
    "pipeline": [
      [
        "@pika/plugin-standard-pkg",
        {
          "exclude": [
            "__tests__/**/*"
          ]
        }
      ],
      [
        "@pika/plugin-build-node"
      ],
      [
        "@pika/plugin-simple-bin",
        {
          "bin": "gen-webext-manifest"
        }
      ]
    ]
  },
  "scripts": {
    "build": "pika build",
    "test": "echo 'Hopefully it works'",
    "lint": "eslint src/**/*.js"
  },
  "dependencies": {
    "@spurtli/text-to-svg": "^1.2.2",
    "app-root-path": "^3.0.0",
    "yargs": "^14.2.0"
  },
  "devDependencies": {
    "@commitlint/config-conventional": "^8.1.0",
    "@pika/pack": "^0.5.0",
    "@pika/plugin-build-node": "^0.6.1",
    "@pika/plugin-simple-bin": "^0.6.1",
    "@pika/plugin-standard-pkg": "^0.6.1",
    "commitlint": "^8.1.0",
    "eslint": "^6.5.1",
    "jest": "^24.9.0"
  },
  "commitlint": {
    "extends": [
      "@commitlint/config-conventional"
    ]
  }
}
